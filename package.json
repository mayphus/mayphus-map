{
  "name": "@mayphus/roadsigil",
  "private": true,
  "packageManager": "bun@1.3.6",
  "type": "module",
  "scripts": {
    "dev": "mkdir -p dist && cp -R public/. dist && clojure -M:shadow watch app",
    "build": "mkdir -p dist && cp -R public/. dist && clojure -M:shadow release app",
    "test": "bun run build"
  },
  "dependencies": {
    "maplibre-gl": "^5.7.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "scheduler": "^0.23.2"
  }
}
